{{#each tray.tasks as |task|}}
  {{#accordion-multi-form-item task=task active=(eq selected task) selectTaskAction="selectTask" removeTaskAction="removeTask"}}
    {{yield task}}
  {{/accordion-multi-form-item}}
{{/each}}

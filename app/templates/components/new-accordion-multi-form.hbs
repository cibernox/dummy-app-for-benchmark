{{#each tray.tasks as |task|}}
  {{#new-accordion-multi-form-item task=task active=(eq selected task) selectTaskAction="selectTask" deleteTaskAction="deleteTask"}}
    {{yield task}}
  {{/new-accordion-multi-form-item}}
{{/each}}
